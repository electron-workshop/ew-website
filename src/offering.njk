---
layout: base.njk
title: Offering — {{ site.title }}
permalink: "/offering/"
---
<h1 class="mb-4">What you get by joining</h1>
<p class="text-secondary mb-4">Small groups shouldn’t have to manage servers, SaaS bills, and tool sprawl. Join our network and use our maintained, privacy‑respecting stack. Each group gets their own login for the services below.</p>

<style>
  .offering-logo{ max-height: 36px; width: auto; }
</style>

<div class="row g-4">
  {% for item in offerings %}
    <div class="col-12 col-md-6 col-lg-4">
      <div class="card h-100 border-0 shadow-sm hover-card">
        <div class="card-body d-flex flex-column">
          <div class="d-flex align-items-center gap-2 mb-2">
            {% if item.logo %}
              <img src="{{ item.logo }}" alt="{{ item.title }} logo" class="offering-logo">
            {% endif %}
            <h2 class="h5 mb-0">{{ item.title }}</h2>
          </div>
          <p class="flex-grow-1">{{ item.text }}</p>
          <a href="/offering/{{ item.slug }}/" class="stretched-link" aria-label="{{ item.title }}">Read more</a>
        </div>
      </div>
    </div>
  {% endfor %}
</div>

<div class="text-center mt-5">
  <p class="lead mb-2">Interested in joining?</p>
  <p class="mb-0">Email <strong>network[at]electronworkshop.com.au</strong> and we’ll help you get started.</p>
</div>
