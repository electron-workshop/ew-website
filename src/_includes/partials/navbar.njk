<nav
  class="navbar navbar-expand-lg shadow-sm sticky-top"
  style="background: var(--surface)"
>
  <div class="container">
    <a class="navbar-brand d-flex align-items-center gap-2" href="/">
      <img
        src="/assets/images/logo.jpg"
        alt="Electron Workshop"
        width="132"
        height="56"
        class="rounded"
      />
      {# <span class="fw-semibold">{{ site.title }}</span> #}
    </a>

    {#
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#nav"
      aria-controls="nav"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div id="nav" class="collapse navbar-collapse">
      <ul class="navbar-nav ms-auto">
        <li class="nav-item">
          <a
            class="nav-link{% if page.url == '/' %} active{% endif %}"
            href="/"
            >Home</a
          >
        </li>
        <li class="nav-item">
          <a
            class="nav-link{% if page.url == '/offering/' %} active{% endif %}"
            href="/offering/"
            >Offering</a
          >
        </li>
      </ul>
    </div>
    #}

    <button
      class="btn btn-outline-primary border-0 rounded-3 fs-5 fw-bold"
      onclick="openSearch()"
    >
      <i class="bi bi-search me-2"></i>Search
    </button>
  </div>
</nav>

<!-- Search Modal -->
<div
  class="modal fade"
  id="searchModal"
  tabindex="-1"
  aria-labelledby="searchModalLabel"
  aria-hidden="true"
>
  <div class="modal-dialog modal-lg modal-dialog-centered">
    <div class="modal-content border-0 shadow-lg">
      <div class="modal-header border-0 pb-0">
        <h5 class="modal-title text-primary fw-semibold" id="searchModalLabel">
          <i class="bi bi-search me-2"></i>Search Events & Content
        </h5>
        <button
          type="button"
          class="btn-close"
          data-bs-dismiss="modal"
          aria-label="Close"
        ></button>
      </div>
      <div class="modal-body pt-2">
        <div class="mb-3">
          <input
            type="text"
            class="form-control form-control-lg border-primary"
            id="searchInput"
            placeholder="Search events, tutorials, discussions..."
            oninput="performSearch()"
          />
        </div>
        <div
          id="searchResults"
          class="search-results"
          style="max-height: 400px; overflow-y: auto"
        >
          <!-- Results will appear here -->
        </div>
      </div>
    </div>
  </div>
</div>
