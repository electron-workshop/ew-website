{
  "name": "eleventy-bootstrap-site",
  "version": "1.1.0",
  "private": true,
  "scripts": {
    "dev": "npx @11ty/eleventy --serve --quiet",
    "build": "npx @11ty/eleventy",
    "start": "npm run dev"
  },
  "dependencies": {
    "@11ty/eleventy": "^3.0.0",
    "luxon": "^3.5.0"
  }
}